<div class="container-fluid margin-top-70">
        {{ form_start(form) }}
        <div class="row">
            <div class="offset-md-8 col-md-3 px-0">
                {{ form_row(form.category) }}
            </div>
            <div class="col-md-1 px-0">
                <button class="btn btn-success">Search</button>
            </div>
        </div>
        {{ form_end(form) }}
</div>